# Prepare and start sshd
HOST_KEY_PATH="/data/ssh"
HOST_KEY="${HOST_KEY_PATH}/ssh_host_ed25519_key"

mkdir -p "${HOST_KEY_PATH}"
chmod 0700 "${HOST_KEY_PATH}"

if [ ! -f "${HOST_KEY}" ]; then
    ssh-keygen -t ed25519 -N "" -f "${HOST_KEY}"
fi

mkdir -p /var/run/sshd
/usr/sbin/sshd
